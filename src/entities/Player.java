package entities;

import io.Window;
import render.Camera;
import world.World;

public class Player {

	public Player() {
		
	}
	
	public void update(float delta, Window window, Camera camera, World world) {
		
	}
}
